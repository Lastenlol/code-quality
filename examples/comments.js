// db hard work
function sendNotificationsToUsers(users) { // send notifications to users
    const notificationMessage = 'Hello';  // I’m really necessary comment

    for (var i = 0; i < users.length; i++) { // iterate me, yeaah
        sendMessageToUser(users[i], message); // what the message?
    }
    for (var i = 0; i < users.length; i++) { // MORE FOREACHS
        sendCoockies(users[i]) // (it’s not a typo, dear perv) sending cookies to user
    }
}
